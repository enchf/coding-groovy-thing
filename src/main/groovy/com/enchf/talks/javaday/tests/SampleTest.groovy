package com.enchf.talks.javaday.tests

import spock.lang.Specification

class SampleTest extends Specification {
    def "Sample test before building real ones" () {
        given:
        "No value is given yet"

        when:
        "Here we execute functions to be tested"

        then:
        "A block with boolean conditions to assert"
        true
    }
}
